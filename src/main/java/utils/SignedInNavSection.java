package utils;

/**
 * Which primary nav row is active for the shared signed-in sidebar (visual only).
 */
public enum SignedInNavSection {
    NONE,
    HOME,
    OFFERS,
    AGENCIES,
    MESSAGES,
    RECOMMENDATIONS,
    EVENTS,
    PROFILE
}
